datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Venda {
  id        Int      @id @default(autoincrement())
  produto   String
  categoria String
  valor     Float
  data      DateTime
}
